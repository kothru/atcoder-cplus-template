#include <iostream>
using namespace std;

int main()
{
  bool is = true;
  int a = 1;
  // declare var first
  int a, b;
  // input
  cin >> a >> b;
  // output
  cout << a << endl;
  // forloop
  int N;
  for (int n = 1; n < N; ++n)
  {
  }
  // while
  while (N > 0 && N < 0)
  {
  }
  a /= 10;
  a = b % 10;
}

int calc(int n)
{
}